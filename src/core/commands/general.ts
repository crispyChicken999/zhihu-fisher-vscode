import * as vscode from 'vscode';

/**
 * 注册通用命令
 */
export function registerGeneralCommands(): vscode.Disposable[] {
  const commands: vscode.Disposable[] = [];
  
  // 这里可以添加一些不属于特定类别的通用命令
  
  return commands;
}
